# Prometheus server configuration for TensorFlow Serving monitoring.
# The scrape target and metrics path are configured in prometheus.yml.

storage.tsdb.retention.time=15d
web.listen-address=0.0.0.0:9090
web.enable-lifecycle=true
